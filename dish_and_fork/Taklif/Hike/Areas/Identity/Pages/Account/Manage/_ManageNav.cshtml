@using Hike.Areas.Identity.Pages.Account.Manage
@using Hike.Entities
@inject SignInManager<HikeUser> SignInManager
@{
    var hasExternalLogins = (await SignInManager.GetExternalAuthenticationSchemesAsync()).Any();
    //var user = await SignInManager.UserManager.GetUserAsync(User);
    //var isSeller = await SignInManager.UserManager.IsInRoleAsync(user, "seller");
}
<ul class="nav nav-pills flex-column">
    <li class="nav-item"><a class="nav-link @ManageNavPages.IndexNavClass(ViewContext)" id="profile" asp-page="./Index">Profile</a></li>
    <li class="nav-item"><a class="nav-link @ManageNavPages.EmailNavClass(ViewContext)" id="email" asp-page="./Email">Email</a></li>
    <li class="nav-item"><a class="nav-link @ManageNavPages.ChangePasswordNavClass(ViewContext)" id="change-password" asp-page="./ChangePassword">Password</a></li>
    @if (hasExternalLogins)
    {
        <li id="external-logins" class="nav-item"><a id="external-login" class="nav-link @ManageNavPages.ExternalLoginsNavClass(ViewContext)" asp-page="./ExternalLogins">External logins</a></li>
    }
    <li class="nav-item"><a class="nav-link @ManageNavPages.PersonalDataNavClass(ViewContext)" id="personal-data" asp-page="./PersonalData">Personal data</a></li>
@*@if (!isSeller)
{
    <li class="nav-item"><a class="nav-link @ManageNavPages.IsSellerNavClass(ViewContext)" id="is-seller" asp-page="./IsSeller">Стать продавцом</a></li>
}*@
</ul>
